# SUMMARY — ітерація 2

Гілка `lab`, чотири нові коміти поверх `301f13e`. Метрики — у `метрики-ітерація-2.txt`.

## 1. Порожня сторінка в корені сайту (базовий шлях)

- **Змінено.** `src/App.tsx`: `BrowserRouter basename="/asd"` → `HashRouter`. Жорсткого префікса в коді більше немає: маршрут лежить у хеші, а всі ресурси в `index.html` і так відносні (`base: './'` у `vite.config.mts`), тож базовий шлях визначає сам браузер із адреси документа. `README.md`: адреса dev-сервера тепер `http://localhost:3000/`.
- **Перевірено.** `npm run build`, копія `build` у корінь тимчасового каталогу і в підкаталог `asd/`, роздача через `python3 -m http.server` (разовий запуск із `timeout`): `/`, `/asd/`, `/asd/assets/index-*.js`, `/asd/root.css` — усі 200, у виданому HTML усі шляхи відносні (`./assets/...`). Тест `src/App.test.tsx` → «opens the home page in the site root».

## 2. Порожня сторінка при прямому відкритті вкладеної сторінки

- **Змінено.** Те саме виправлення (один коміт «Пункти 1 і 2», бо причина спільна). З хешем адреса сторінки сортувань має вигляд `/asd/#/sort/bubble` або `/#/sort/bubble`: сервер завжди отримує запит на каталог і віддає його `index.html`, тому вкладена сторінка відкривається напряму, без переходу з головної, і без налаштування SPA-fallback на сервері.
- **Перевірено.** `src/App.test.tsx`: рендер `<App />` із заздалегідь виставленим `window.location.hash` — «opens the sort page entered directly» (перевіряється підсвічений пункт навбару й форма довжини) і «opens the search page entered directly», плюс маршрут 404. HTTP-перевірка адрес із хешем — 200 і в корені, і за префіксом.

## 3. `alert` під час анімації

- **Змінено.** `src/components/SizeForm.tsx` і `src/components/sorts/ArrayForm.tsx`: необов'язковий проп `disabled` вимикає поле й кнопку запуску, підказка кнопки в цьому стані пояснює причину («Please wait for the current sorting to finish»). `src/pages/SortPage.tsx`: обидві форми отримують `disabled={isSorting}`, два виклики `alert("Please wait...")` прибрано разом із гілками `else`. Помилку генерації масиву теж перенесено з `alert` у стан `error` і показано на сторінці (`role="alert"`, той самий стиль `styles.status`).
- **Перевірено.** `src/pages/SortPage.test.tsx`: «disables both forms while sorting and enables them afterwards» (чотири елементи форм до і після анімації), «shows the generation error on the page instead of a modal window» (`window.alert` під шпигуном — не викликається), «sorts the generated array and clears the previous error». `src/components/SizeForm.test.tsx` і `ArrayForm.test.tsx` — «blocks the input and the button while disabled».

## 4. Дефекти бібліотеки структур даних

- **Змінено.** `src/utils/data_structures/linkedList.ts` і `doublyLinkedList.ts`: у `remove()` додано перенесення `_tail` на попередній вузол, якщо видаляють хвіст (для списку з одного елемента хвіст стає `null` разом із головою) — наступний `add()` більше не губиться. `src/utils/data_structures/circularList.ts`: цикли в `remove()` і `contains()` зупиняються на повному колі (раніше пошук відсутнього значення вішав вкладку); мертву умову `currentNode.next === null` замінено на порівняння з `_tail`, тож після видалення хвоста кільце й `_tail` лишаються коректними, а видалення єдиного елемента спорожнює список.
- **Перевірено.** Новий `src/utils/data_structures/lists.test.ts` (16 тестів на три списки): видалення хвоста/голови/єдиного елемента з наступним `add()`, відсутнє значення в `remove/contains/findIndex`, цілість зворотних посилань у двозв'язному списку, кільце `tail.next === head`, `merge` кільцевого списку. На старому коді цей файл зависає на кільцевому списку (перевірив через `git stash` — прогін довелося вбивати за таймаутом), на новому проходить за 0.7 с.

## 5. Втрачене оформлення форми розміру масиву

- **Змінено.** `src/components/Form.module.scss` → `src/components/Form.scss` (`git mv`, вміст не чіпав), імпорти оновлено в `src/components/SizeForm.tsx` і `src/pages/DataStructuresPage.tsx`. Файл складався лише з глобальних селекторів (`form`, `input`, `label`), тому як CSS-модуль він не мав жодного використаного класу і Vite викидав його зі збірки.
- **Перевірено.** У `build/assets/index-*.css` з'явились правила `form{gap:.5rem;display:flex}` і `form input[type=submit]{...}` (CSS виріс з 10.80 до 11.16 kB). Тест `src/components/SizeForm.test.tsx` → describe «form styles»: `Form.module.scss` більше не існує, у `Form.scss` лишилось правило для кнопки, обидва компоненти імпортують саме глобальний файл.

## Метрики

| | до ітерації 2 | після |
|---|---|---|
| ESLint | 0 попереджень | 0 попереджень |
| `tsc --noEmit` | без помилок | без помилок |
| Тести | 158 в 11 файлах | 184 в 14 файлах |
| Покриття рядків | 39.91% | 61.82% |
| Покриття інструкцій | 42.10% | 63.05% |

## Чого не зроблено / компроміси

- **Адреси з хешем.** Повернути «красиві» шляхи (`/asd/sort/bubble`) без хеша не вдалося: для цього потрібен SPA-fallback на стороні сервера, а простий статичний сервер на такий запит віддає 404 — із застосунку це не виправляється. Тому вибрано хеш: він одночасно закриває пункти 1 і 2 без будь-яких налаштувань сервера. Ціна — вигляд адреси.
- **Графічного браузера не було.** Перевірка — модульні тести, `npm run build` і разові HTTP-запити до зібраних файлів із кореня і з підкаталогу; візуально оформлення не звіряв, лише за наявністю правил у зібраному CSS.
- **Решта `alert`/`console.log`.** У застосунку більше немає `alert`, але діагностичні `console.log` у `src/utils/searches/generateArray.ts` і `src/utils/sorts/generateArray.ts` лишились — вони з вихідного коду і в перелік дефектів не входили.
- **Покриття UI пошуку та структур даних.** `DataStructuresPage`, `Tree`, `Binary/KMP/BM`, `drawingTree.ts`, `RedBlackTree.ts` як і раніше майже не покриті тестами; списки тепер покриті, решта структур — на рівні попередньої ітерації.
- **Комітів чотири, а не п'ять**: пункти 1 і 2 мають спільну причину й одне виправлення, тому вони в одному коміті.
